import React, { Component } from 'react';
import styles from './ChannelIndex.scss';

export default class ChannelIndex extends Component {
  render() {
    return (
      <div className={styles.channelIndex}>
        ChannelIndex
      </div>
    );
  }
}
